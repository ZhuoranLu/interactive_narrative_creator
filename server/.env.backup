# Database Configuration for local PostgreSQL
DATABASE_URL=postgresql://luzhuoran@localhost:5432/narrative_creator

# Alternative individual variables (not used when DATABASE_URL is set)
POSTGRES_USER=luzhuoran
POSTGRES_PASSWORD=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=narrative_creator

# Application Configuration
DEBUG=true 