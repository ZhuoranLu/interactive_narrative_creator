# Database Configuration
# Option 1: Use complete DATABASE_URL (recommended for production)
# DATABASE_URL=postgresql://username:password@localhost:5432/narrative_creator

# Option 2: Use individual PostgreSQL environment variables
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=narrative_creator

# Application Configuration
DEBUG=false

# For development with Docker
# POSTGRES_HOST=db  # Use service name when running with docker-compose 